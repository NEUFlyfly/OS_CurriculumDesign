# UnixFileSystem 项目介绍

## 项目概述

`UnixFileSystem` 是一个操作系统课程设计性质的 C++ 项目，用一个普通文件 `data.img` 作为虚拟磁盘，在其上模拟 Unix 风格的文件系统。项目围绕超级块、inode、目录项、位图、磁盘块分配、用户登录、权限检查和命令行交互等内容展开，适合用于理解文件系统的基本组织方式和常见操作流程。

程序启动后会初始化虚拟磁盘镜像，必要时格式化文件系统，然后要求用户登录。登录成功后，用户可以在类似 shell 的命令行界面中执行目录、文件、用户、权限和文本编辑相关命令。

## 运行环境与构建方式

工程使用 CMake 构建，`CMakeLists.txt` 中声明的最低 CMake 版本为 `3.13`，项目名为 `UnixFileSystem`，语言标准为 C++17。构建目标是同名可执行程序 `UnixFileSystem`。

工程还通过 `link_libraries(-lncurses)` 链接 `ncurses`，这是项目中文本编辑器界面使用的终端控制库。因此，运行或构建前需要确保本机编译环境能够找到并链接 `ncurses`。

在项目目录下，典型构建流程如下：

```bash
cmake -S . -B build
cmake --build build
```

由于项目依赖 `clear` 命令和 `ncurses` 终端能力，实际运行环境应具备类 Unix 终端能力或等价支持。

## 工程目录结构

项目根目录主要包含以下内容：

| 路径 | 作用 |
| --- | --- |
| `CMakeLists.txt` | 定义 CMake 项目、C++17 标准、`ncurses` 链接和可执行目标 |
| `main.cpp` | 程序入口，负责初始化镜像、安装文件系统、登录和命令循环 |
| `include/` | 头文件目录，定义常量、核心数据结构和类接口 |
| `src/` | 源文件目录，实现镜像、文件系统和编辑器逻辑 |
| `README.md` | 当前仅包含项目标题 `UnixFileSystem` |

`include/` 下的主要头文件包括：

| 文件 | 说明 |
| --- | --- |
| `const.h` | 文件系统常量，如块大小、inode 数量、镜像文件名等 |
| `superblock.h` | 定义超级块 `SuperBlock` |
| `inode.h` | 定义 inode 结构 `iNode` |
| `dir.h` | 定义目录项结构 `Dir` |
| `image.h` | 定义虚拟磁盘镜像类 `Image` |
| `filesystem.h` | 定义文件系统核心类 `FileSystem` |
| `editor.h` | 定义终端文本编辑器相关结构和移动函数 |

`src/` 下的实现文件包括：

| 文件 | 说明 |
| --- | --- |
| `image.cpp` | 负责打开或创建 `data.img` 虚拟磁盘文件 |
| `filesystem.cpp` | 实现格式化、登录、目录文件操作、权限、用户管理和命令解析 |
| `editor.cpp` | 实现基于 `ncurses` 的文本编辑辅助结构与光标移动逻辑 |

## 程序启动流程

程序入口位于 `main.cpp`，整体流程如下：

1. 调用 `system("clear")` 清屏。
2. 创建 `Image` 对象，并调用 `InitImage(isFormat)` 初始化虚拟磁盘镜像。
3. 创建 `SuperBlock` 和 `FileSystem` 对象。
4. 如果镜像文件是首次创建或需要初始化，则调用 `FileSystem::Format()` 格式化文件系统。
5. 调用 `FileSystem::InitFileSystem()` 从镜像中读取超级块、inode 位图和 block 位图。
6. 循环调用 `FileSystem::Login()`，直到用户登录成功。
7. 登录后进入命令循环：显示提示符，读取用户输入，并交给 `FileSystem::ReadCommand()` 解析执行。
8. 退出后关闭镜像文件读写指针，并输出登出提示。

这说明该项目不是直接操作宿主机真实文件系统，而是通过 `data.img` 保存模拟文件系统的状态。

## 虚拟磁盘与文件系统布局

文件系统的重要常量定义在 `include/const.h` 中：

| 常量 | 值 | 含义 |
| --- | --- | --- |
| `BLOCK_SIZE` | `512` | 每个磁盘块大小为 512 字节 |
| `INODE_SIZE` | `128` | 每个 inode 大小为 128 字节 |
| `MAX_NAME_SIZE` | `28` | 文件名或目录名最大长度限制 |
| `INODE_NUM` | `640` | inode 总数 |
| `BLOCK_NUM` | `10240` | 磁盘块总数 |
| `BLOCKS_PER_GROUP` | `64` | 成组链接法中每组 block 数量 |
| `IMAGEFILENAME` | `"data.img"` | 虚拟磁盘镜像文件名 |
| `HOSTNAME` | `"chaosmyth"` | 命令提示符中的主机名 |

文件系统在镜像文件中规划了超级块、inode 位图、block 位图、inode 区和数据块区等区域。相关起始地址由 `SUPERBLOCK_START_ADDR`、`INODEBITMAP_START_ADDR`、`BLOCKBIITMAP_START_ADDR`、`INODE_START_ADDR`、`BLOCK_STARTADDR` 等宏定义给出。

格式化时，`FileSystem::Format()` 会初始化超级块字段、清空 inode 和 block 位图，并按成组链接法组织空闲磁盘块。随后会创建根目录 `/`，并建立 `/home`、`/home/root`、`/etc` 等初始目录和用户配置文件。

## 核心数据结构

### SuperBlock

`SuperBlock` 定义在 `include/superblock.h` 中，用来记录文件系统整体状态，包括 inode 总数、block 总数、空闲 inode 数、空闲 block 数、空闲块栈、块大小、inode 大小以及各区域在镜像文件中的起始地址。

它是文件系统元数据的核心，格式化时会写入镜像开头，初始化文件系统时再从镜像中读取回来。

### iNode

`iNode` 定义在 `include/inode.h` 中，用来描述文件或目录。主要字段包括：

- `inode_id`：inode 编号。
- `inode_mode`：类型和权限位。
- `inode_cnt`：目录项或文件名引用计数。
- `user_name`、`user_group`：所属用户和用户组。
- `inode_size`：文件大小或目录占用大小。
- `inode_dirblock[10]`：10 个直接块地址。
- `inode_indirect_block_first`：一级间接块字段，当前代码中初始化为 `-1`。

源码注释中说明该 inode 采用混合索引思路，但当前主要实现使用 10 个直接块。

### Dir

`Dir` 定义在 `include/dir.h` 中，是目录项结构，包含文件或目录名 `name` 和对应 inode 地址 `inode_addr`。注释说明目录项大小为 32 字节，一个 512 字节 block 可存放 16 个目录项。

目录创建时会写入 `.` 和 `..` 两个目录项，分别指向当前目录 inode 和父目录 inode。

## 主要功能模块

### 虚拟磁盘镜像模块

`Image` 类负责管理虚拟磁盘文件名和读写文件指针。`Image::InitImage()` 会尝试以二进制方式打开 `data.img`：如果文件不存在，则创建它并标记需要格式化；如果文件存在，则以读和读写方式打开。

### 文件系统模块

`FileSystem` 是项目核心类，负责：

- 文件系统格式化与初始化。
- inode 和 block 分配、释放。
- 目录创建、查找、显示和删除。
- 文件创建、删除、读取和写入。
- 用户登录、用户新增和删除。
- 权限检查和 `chmod` 修改。
- 命令行提示符生成和命令解析。

### 空间管理

inode 空闲状态通过 `inode_bitmap` 管理，block 空闲状态通过 `block_bitmap` 管理。block 分配还结合超级块中的空闲块栈，体现了成组链接法管理空闲块的思路。

### 权限管理

代码中用 `inode_mode` 同时表示文件类型和权限。目录通过 `MODE_DIR` 标识，普通文件通过 `MODE_FILE` 标识。默认文件权限为 `0664`，默认目录权限为 `0755`。执行目录、文件读写、删除和编辑操作时，程序会根据当前用户、用户组和权限位进行检查；`root` 用户在多处逻辑中拥有特殊权限。

## 命令行交互功能

命令解析在 `FileSystem::ReadCommand()` 中实现，帮助文本由 `FileSystem::Help()` 输出。当前支持的命令包括：

| 命令 | 功能 |
| --- | --- |
| `ls` | 列出当前目录内容 |
| `cd [directoryName]` | 切换当前目录 |
| `mkdir [directoryName]` | 创建子目录 |
| `rmdir [directoryName]` | 删除子目录 |
| `nano [filename]` | 编辑文件，不存在时可创建 |
| `touch [filename]` | 创建空文件 |
| `rm [filename]` | 删除文件 |
| `cat [filename]` | 输出文件内容 |
| `chmod [filename] [permissions]` | 修改文件或目录权限 |
| `pwd` | 输出当前目录路径 |
| `clear` | 清空终端显示 |
| `exit` | 退出当前登录状态或系统命令循环 |
| `useradd [username]` | 添加用户 |
| `userdel [username]` | 删除用户 |
| `format` | 格式化文件系统，代码中限制为 `root` 用户执行 |
| `help` | 输出命令帮助信息 |

命令提示符由 `FileSystem::MakeMenu()` 生成，格式中包含当前用户名、主机名 `chaosmyth` 和当前目录路径。

## 用户与权限管理

格式化文件系统时，程序会创建基础用户和配置文件：

- `/home/root`：root 用户目录。
- `/etc/user`：保存用户、UID、GID 等信息。
- `/etc/passwd`：保存用户名和密码。
- `/etc/group`：保存用户组信息。

初始 root 用户由格式化逻辑写入，`passwd` 中保存的初始记录为 `root:root`。登录时，程序会进入 `/etc` 查找 `user` 和 `passwd` 文件，校验用户是否存在以及密码是否匹配。登录成功后，当前目录会切换到对应用户的家目录。

`useradd` 和 `userdel` 只允许 `root` 用户执行。新增用户时，程序会在 `/home` 下创建对应用户目录，并更新 `/etc/user`、`/etc/passwd` 和 `/etc/group`。删除用户时，程序会尝试从这些配置文件中移除用户记录，并删除对应用户目录。

## 文本编辑功能

项目提供了 `nano [filename]` 命令进入文本编辑流程。编辑器相关逻辑分布在 `include/editor.h`、`src/editor.cpp` 和 `FileSystem::editor()` 中。

编辑器使用 `ncurses` 初始化终端界面，支持方向键移动、退格删除、Tab 插入空格、回车插入新行等操作。底部状态栏提示 `F4:Exit` 和 `F5:Save&Exit`：

- 按 `F4` 退出编辑器，不保存。
- 按 `F5` 保存并退出。

如果编辑的是已有文件，保存时会检查写权限并调用 `WriteFile()` 覆盖文件内容；如果文件不存在，则在当前目录有写权限时调用 `Create()` 创建新文件。

## 项目特点与局限

项目的主要特点包括：

- 用镜像文件模拟磁盘，便于观察文件系统元数据和数据区组织。
- 实现了超级块、inode、目录项、位图和空闲块管理等文件系统基础机制。
- 支持类似 Unix shell 的交互命令，覆盖目录、文件、权限和用户管理。
- 提供基于 `ncurses` 的简易文本编辑功能。
- 在课程设计层面展示了 Unix 风格文件系统的核心思想。

需要注意的局限包括：

- 当前 `README.md` 只有项目标题，本介绍文档主要依据源码整理。
- 项目没有在源码中体现自动化测试或跨平台兼容性说明。
- 文件数据主要通过 inode 中的 10 个直接块访问，一级间接块字段存在但当前实现中未形成完整使用路径。
- 代码中存在部分调试辅助文件写入逻辑，例如编辑器调试输出 `afile.txt`、`bfile.txt`。
- 该项目适合作为课程设计和学习示例，不应直接理解为生产级文件系统实现。

## 事实依据

本文档依据以下源码文件整理：

| 文件 | 提供的事实依据 |
| --- | --- |
| `CMakeLists.txt` | CMake 版本、项目名、C++17、`ncurses` 链接和可执行目标 |
| `main.cpp` | 程序启动、镜像初始化、格式化、登录、命令循环和退出流程 |
| `include/const.h` | 文件系统常量、镜像文件名、主机名、区域起始地址 |
| `include/superblock.h` | 超级块结构字段 |
| `include/inode.h` | inode 结构字段和直接块设计 |
| `include/dir.h` | 目录项结构和目录项大小说明 |
| `include/image.h`、`src/image.cpp` | 虚拟磁盘镜像文件的打开与创建逻辑 |
| `include/filesystem.h`、`src/filesystem.cpp` | 文件系统格式化、初始化、命令解析、文件目录操作、用户与权限管理 |
| `include/editor.h`、`src/editor.cpp` | 编辑器数据结构、页面行管理和光标移动逻辑 |
