# FlyflyUFS 命令测试文档（精简版）

## 1. 测试范围

本文档只测试命令功能是否可用，不测试构建脚本、前端样式、快捷键、拖拽、源码单元测试或文件系统内部结构。

测试入口：

- CLI 交互式命令行。
- 前端终端面板。
- 少量 WebSocket 命令接口。

原则：只测必要主流程；异常情况只选 2 到 3 个代表用例，不逐条命令穷举。

## 2. 测试前置条件

1. 使用测试用 `data.img`，避免污染已有数据。
2. 默认使用 root 登录：

```text
username: root
password: root
```

3. root 登录后当前目录应为：

```text
/home/root
```

4. 测试临时对象统一使用：

```text
cmd_dir
a.txt
testuser
```

## 3. CLI 必要测试

| 编号 | 测试内容 | 命令 / 步骤 | 预期结果 |
| --- | --- | --- | --- |
| CLI-001 | 登录与基础命令 | 登录 root 后执行 `pwd`、`ls`、`help` | `pwd` 输出 `/home/root`；`ls` 可列出目录；`help` 包含主要命令 |
| CLI-002 | 目录主流程 | `mkdir cmd_dir` → `ls` → `cd cmd_dir` → `pwd` → `cd ..` → `rmdir cmd_dir` | 目录可创建、进入、返回并删除 |
| CLI-003 | 文件主流程 | `touch a.txt` → `nano a.txt` 写入 `hello` 并保存 → `cat a.txt` → `chmod a.txt 0600` → `rm a.txt` | 文件可创建、编辑、读取、改权限并删除 |
| CLI-004 | 用户主流程 | root 执行 `useradd testuser`，密码设为 `123456`；退出后用新用户登录；最后 root 执行 `userdel testuser` | 用户可创建、登录、删除；新用户默认目录为 `/home/testuser` |
| CLI-005 | 代表性缺参 | 执行 `chmod a.txt` | 输出 `usage: chmod [filename] [permissions] : Change the file permissions` |
| CLI-006 | 代表性不存在对象 | 执行 `cat missing.txt` | 输出 `cat missing.txt : No such file` |
| CLI-007 | 代表性权限限制 | 普通用户执行 `useradd otheruser` | 输出 `非root用户, 不能添加新用户` |
| CLI-008 | 未知命令 | 执行 `abc` | 输出 `command not found: abc` |
| CLI-009 | 退出 | 执行 `exit` | 当前会话退出 |

## 4. 前端终端必要测试

前端终端不需要重复测试 CLI 的每个细节，只验证前端命令入口、编辑器适配、用户切换和基本错误处理。

| 编号 | 测试内容 | 命令 / 步骤 | 预期结果 |
| --- | --- | --- | --- |
| WEB-001 | 登录与提示符 | 使用 `root` / `root` 登录前端终端 | 提示符显示当前用户和路径，例如 `root@FlyflyUFS /home/root>` |
| WEB-002 | 基础命令 | 输入 `pwd`、`ls`、`help` | 能正常输出路径、目录和帮助信息 |
| WEB-003 | 目录和文件主流程 | `mkdir cmd_dir` → `cd cmd_dir` → `touch a.txt` → `nano a.txt` 保存 `hello web` → `cat a.txt` | 目录、文件和前端 `nano` 编辑均可用 |
| WEB-004 | 编辑大小限制 | `nano a.txt` 后输入超过 `5120 B` 的内容并保存 | 前端拒绝保存并提示超过大小限制 |
| WEB-005 | 用户创建与切换 | 输入 `useradd testuser`，设置密码后确认切换 | 自动切换到新用户，提示符显示 `testuser@FlyflyUFS ...` |
| WEB-006 | 清理与退出 | 切回 root 后删除 `testuser`、删除测试文件和目录，再执行 `exit` | 测试对象被清理；`exit` 返回登录界面且服务端不关闭 |
| WEB-007 | 代表性异常 | 输入 `cat missing.txt` 或 `abc` | 返回对应错误，不导致页面异常 |

## 5. WebSocket 必要接口测试

WebSocket 只做接口连通性和关键能力验证，不逐条覆盖所有命令。

调用示例：

```js
WSClient.send('ls', {})
```

| 编号 | 测试内容 | 调用 / 步骤 | 预期结果 |
| --- | --- | --- | --- |
| WS-001 | 登录与查询 | `login` root 后调用 `pwd`、`ls` | 登录成功，能返回当前路径和目录内容 |
| WS-002 | 文件与目录 | 依次调用 `mkdir`、`cd`、`touch`、`write`、`cat`、`rm`、`rmdir` | 文件和目录可通过接口完成基本增删改查 |
| WS-003 | 用户接口 | 调用 `useradd`，参数含 `username` 和 `password`，再调用 `userdel` | 用户可创建并删除，`useradd` 不阻塞等待终端输入 |
| WS-004 | 存储信息 | 调用 `disk_usage`、`storage_inspection` | 返回磁盘和文件系统检查信息 |
| WS-005 | 代表性保护 | 调用 `format` 但不传 `confirm: 'FORMAT'` | 返回需要确认，不执行格式化 |
| WS-006 | 关闭服务 | 调用 `shutdown` | 返回关闭成功信息，服务端随后停止；此项必须最后执行 |

## 6. 破坏性命令测试

`format` 会清空文件系统，只能在测试镜像上执行。

| 编号 | 测试内容 | 命令 / 步骤 | 预期结果 |
| --- | --- | --- | --- |
| DEST-001 | 取消格式化 | 前端终端输入 `format`，确认时不输入 `FORMAT` | 不格式化，数据保留 |
| DEST-002 | 执行格式化 | root 在 CLI 执行 `format`，或前端输入 `FORMAT` 确认 | 文件系统重新初始化，当前会话退出 |
| DEST-003 | 格式化后验证 | 重新启动并用 `root` / `root` 登录，执行 `pwd` | 登录成功，路径为 `/home/root` |

## 7. 通过标准

- 常用命令能完成文件、目录、用户、权限的核心操作。
- 前端终端能正确显示当前用户，并能在创建用户后切换到新用户。
- 抽测的异常命令能返回错误提示，不导致 CLI、前端或服务端异常退出。
- `format`、`shutdown` 只在测试镜像或测试环境中执行，并放在最后测试。
